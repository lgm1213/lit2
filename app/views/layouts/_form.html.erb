<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
        <%= link_to "LIT", root_path,{ :class=>"navbar-brand"} %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li class="active"></li>
      </ul>
        <% if current_user %>
      <ul class="nav navbar-nav navbar-right">
        <form class="navbar-form navbar-left">
          <div class="input-group">
            <input type="text" class="form-control" placeholder="City or Zip">
            <div class="input-group-btn">
              <button class="btn btn-default" type="submit">
                <i class="glyphicon glyphicon-search"></i>
              </button>
            </div>
          </div>
      </form>
        <li><%= link_to "My Profile", current_user%></li>
        <li><%= link_to "Logout", logout_path, method: "destroy" %></li>
        <% else %>
        <li><%= link_to " Register", signup_path, { :class=>"glyphicon glyphicon-user"} %></li>
        <li><%= link_to " Login", login_path, { :class=>"glyphicon glyphicon-user"} %></li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>
